# ==========================================
# DepanceAPP Configuration
# ==========================================

# Port exposed on host (Default: 3000)
APP_PORT=3000

# Public URL of the application
# Used for CORS and potential external links.
# If using a reverse proxy/domain, set this to your public URL (e.g. https://my-finance-app.com)
APP_URL=http://localhost:3000

# Database Connection Configuration
# You can either provide individual variables (Recommended) OR a full DATABASE_URL.

# Option 1: Individual Variables (Easier)
DB_HOST=db
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=depance_db
# DB_SCHEMA=public

# Option 2: Full Connection String (Overrides individual variables if set)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA
# DATABASE_URL="postgresql://postgres:postgres@db:5432/depance_db?schema=public"

# Security (Required)
# Generate a secure string (e.g. `openssl rand -base64 32`)
JWT_SECRET=replace_with_a_long_secure_random_string

# Environment (Optional)
# Options: development, production, test. Default: development
NODE_ENV=production

# Logging (Optional)
# Options: error, warn, info, http, verbose, debug, silly. Default: info
LOG_LEVEL=info

# CORS Allowed Origins (Optional)
# Comma-separated list of allowed origins.
# APP_URL is automatically added to this list.
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# ==========================================
# Backup Configuration (Optional)
#Only needed when using the backup.sh script manually
# ==========================================

# Encryption Key for Backups (Required if using --encrypt)
BACKUP_ENCRYPTION_KEY=your_backup_encryption_key_here

# S3 Backup Configuration (Required if using --s3)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_S3_BUCKET=
# AWS_REGION=

