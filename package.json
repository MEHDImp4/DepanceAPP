{
  "name": "depance-app",
  "version": "1.0.0",
  "description": "Depance Finances App",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently -n server,client \"npm run server\" \"npm run client\"",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "mobile": "npm start --prefix mobile"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}